import { View, Text, Pressable, ScrollView, TouchableOpacity, SafeAreaView } from "react-native"
import { styled } from "nativewind"
import CustomTouchableOpacity from "./CustomTouchableOpacity"

export const TwView = styled(View)

export const TwText = styled(Text, "font-inter-400")

export const TwPressable = styled(Pressable)

export const TwTouchableOpacity = styled(CustomTouchableOpacity)

export const TwScrollView = styled(ScrollView)

export const TwSafeAreaView = styled(SafeAreaView)
